const { ANDROID_HOME, PORT = 3500 } = process.env;

// console.table({ANDROID_HOME, PORT});

const characters = ['Flash', 'Superman', 'Batman', 'Green Lanter'];

const [ , , ,greenLanter  ] = characters;

// console.log(flash)